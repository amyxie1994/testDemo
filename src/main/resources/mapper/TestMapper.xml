<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.assignment.dao.TestMapper">


    <!--mysql-->
    <select id="selectTest"  resultType="java.util.Map">
    SELECT
	t.*
    FROM
	testcrabtable
    </select>

    <insert id="testInsert"   keyProperty="id">
insert  into testcrabtable(testName) values ("honghentest")

    </insert>

</mapper>
